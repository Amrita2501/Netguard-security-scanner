# Copy this file to `.env` in the frontend/ directory to override defaults.
# Vite only exposes variables prefixed with VITE_ to client-side code.

# Base URL of the backend API. Defaults to http://127.0.0.1:8000/api if unset.
VITE_API_BASE_URL=http://127.0.0.1:8000/api
